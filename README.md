List off all patches needed for my build of android on Motorola Razr I

* run `./patch.sh` to apply the patches
* run `./patch.sh --dry-run` to test the patches
* run `./patch.sh -R` to revert the patches
